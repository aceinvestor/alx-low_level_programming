#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *
 * @x: x is an integer
 *
 * Return: 0
 */
int _abs(int x)
{
	for (x = 0 ; x <= 9 ; x++)

	_putchar(x);
	return (0);
}
