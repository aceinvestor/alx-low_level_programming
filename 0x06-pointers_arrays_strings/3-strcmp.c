#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings
 *
 * @s1: string
 * @s2: string
 *
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	int acex;

	acex = strcmp(s1, s2);
	return (acex);
}
